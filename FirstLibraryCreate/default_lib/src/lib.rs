#[cfg(test)]//<callout id="default_lib.cfg" />
mod tests {//<callout id="default_lib.mod" />
  #[test]//<callout id="default_lib.test" />
  fn it_works() {
    let result = 2 + 2;
    assert_eq!(result, 4);//<callout id="default_lib.assert" />
  }
}
